{
  "name": "skellychart",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm run generate:tiling-feasibility",
    "dev": "vite",
    "generate:tiling-feasibility": "tsx scripts/generate-tiling-feasibility.ts",
    "build": "npm run generate:tiling-feasibility && tsc -b && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "tsx": "^4.19.4",
    "typescript": "~6.0.3",
    "vite": "^8.0.13"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
