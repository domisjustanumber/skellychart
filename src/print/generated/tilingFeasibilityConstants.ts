/**
 * AUTO-GENERATED by scripts/generate-tiling-feasibility.ts — do not edit.
 */
export const TILING_FEASIBILITY_GRID_MIN = 2 as const;
export const TILING_FEASIBILITY_GRID_MAX = 40 as const;
export const TILING_FEASIBILITY_GRID_SPAN = 39 as const;
export const TILING_FEASIBILITY_SLOTS_PER_PAPER = 1521 as const;
export const TILING_FEASIBILITY_SQ_MM_MIN = 10 as const;
export const TILING_FEASIBILITY_SQ_MM_MAX = 200 as const;
export const TILING_FEASIBILITY_SQ_BITMAP_BYTES = 24 as const;
export const TILING_FEASIBILITY_PAGE_COUNT_MIN = 1 as const;
export const TILING_FEASIBILITY_PAGE_COUNT_MAX = 9 as const;

const FEASIBILITY_PAPER_ROWS = [{"id":"a4","wMm":210,"hMm":297},{"id":"a3","wMm":297,"hMm":420},{"id":"letter","wMm":215.9,"hMm":279.4},{"id":"tabloid","wMm":279.4,"hMm":431.8}] as readonly {readonly id: string; readonly wMm: number; readonly hMm: number}[];

export function feasibilityPaperIdFromDims(paperWMm: number, paperHMm: number): string | undefined {
    for (const row of FEASIBILITY_PAPER_ROWS) {
        if (row.wMm === paperWMm && row.hMm === paperHMm) {
            return row.id;
        }
    }
    return undefined;
}

export function resolveFeasibilityGridSlot(squaresX: number, squaresY: number): number | undefined {
    if (
        squaresX < TILING_FEASIBILITY_GRID_MIN ||
        squaresX > TILING_FEASIBILITY_GRID_MAX ||
        squaresY < TILING_FEASIBILITY_GRID_MIN ||
        squaresY > TILING_FEASIBILITY_GRID_MAX
    ) {
        return undefined;
    }
    return (squaresX - TILING_FEASIBILITY_GRID_MIN) * TILING_FEASIBILITY_GRID_SPAN +
        (squaresY - TILING_FEASIBILITY_GRID_MIN);
}
